//Unintialized Variable
//let currentResult

let currentResult = 0;

// Uses current value in variable and adding 10 then reassigning the variable
// value
currentResult = currentResult + 10;

outputResult(currentResult, '');
